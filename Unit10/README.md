## Unit 10 - Structures And Unions

### Very Short Questions

What is a struct?

Difference between struct and class?

What is a POD type?

Can a struct have functions?

What is a nested struct?

What is aggregate initialization?

What is typedef with struct?

What is alignment?

What is padding?

What is a union?

### Short Questions

What is a struct?

Difference between struct and class?

What is a POD type?

Can a struct have functions?

What is a nested struct?

What is aggregate initialization?

What is typedef with struct?

What is alignment?

What is padding?

What is a union?

### Code Tracing ( Predict The Output)

1.

```cpp
struct A { int x; };
A a = {5};
cout << a.x;

```

2.

```cpp
struct A { int x = 10; };
A a;
cout << a.x;

```

3.

```cpp
struct A { int x; };
A a;
cout << sizeof(a);
```


### Programming Questions

Create a struct Student.

Store and print student records.

Pass struct to function.

Create nested struct for address.

Use array of structs.

### Conceptual Questions

Create a struct Student.

Store and print student records.

Pass struct to function.

Create nested struct for address.

Use array of structs.?
